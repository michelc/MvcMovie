@model MvcMovie.Models.DirectorViewModel

@{ ViewBag.Title = "Réalisateur / Consulter"; }

<form class="form-horizontal">

    <fieldset>

        <legend>@ViewBag.Title</legend>

        @await Html.PartialAsync("_Display.cshtml", Model)

        <hr>
        <div class="form-group">
            <a asp-action="Edit" asp-route-id="@Model.Director_ID" class="btn btn-secondary">Modifier</a>
            <a asp-action="Index">Annuler</a>
        </div>

    </fieldset>
</form>
